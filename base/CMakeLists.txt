cmake_minimum_required (VERSION 3.4)
project(base)
set(CMAKE_CXX_STANDARD 11)
include_directories(./ ../)
file(GLOB base_SRCS *.cpp */*.cpp *.h */*.h  *.cc */*.cc)
link_libraries(uuid)
add_library(base_static STATIC ${base_SRCS})
